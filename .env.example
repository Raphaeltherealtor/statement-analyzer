# Auth (single-user login gate) — see README "Authentication"
APP_PASSWORD=change-me-to-a-strong-password
SESSION_SECRET=run: openssl rand -base64 32

# Supabase (existing) — datastore for parse jobs and tax workspace
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
